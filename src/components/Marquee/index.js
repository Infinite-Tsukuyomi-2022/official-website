import React from "react";
import styles from "./index.module.scss"

const Marquee = () => {
  return (
    <div className={styles.marquee}>
      <div>
        Marquee in CSSMarquee in CSSMarqueein CSSMarquee in CSSMarquee in
      </div>
    </div>
  );
};

export default Marquee;
